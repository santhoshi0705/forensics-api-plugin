<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>io.jenkins.plugins</groupId>
  <artifactId>sample-plugin</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>hpi</packaging>
  <name>Sample Jenkins Plugin</name>
  <url>https://github.com/your-org/sample-plugin</url>

  <properties>
    <jenkins.version>2.414.3</jenkins.version>
    <java.level>11</java.level>
    <plugin.parent.version>4.74</plugin.parent.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <parent>
    <groupId>io.jenkins.plugin</groupId>
    <artifactId>plugin</artifactId>
    <version>${plugin.parent.version}</version>
  </parent>

  <dependencies>
    <!-- Add any Jenkins plugin dependencies below -->

    <!-- Example: Jenkins plugin dependencies -->
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>workflow-step-api</artifactId>
      <version>2.24</version>
      <scope>provided</scope>
    </dependency>

    <!-- Optional: If you are using annotations like @Extension -->
    <dependency>
      <groupId>org.jenkins-ci</groupId>
      <artifactId>symbol-annotation</artifactId>
      <version>1.23</version>
      <scope>provided</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <!-- Compiler plugin setup for Java 11 -->
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>${java.level}</source>
          <target>${java.level}</target>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <developers>
    <developer>
      <id>your-id</id>
      <name>Your Name</name>
      <email>your@email.com</email>
    </developer>
  </developers>

</project>
